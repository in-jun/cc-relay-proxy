# Agent workspace — never push
.claude/

# Build output
/cc-relay-proxy
/cc-relay-proxy.exe

# Runtime state (contain tokens/logs — never commit)
logs/
accounts.json

# Go toolchain
*.test
*.out
*.html

# Env / secret files
.env
*.env
